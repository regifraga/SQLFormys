// Arquivo de configuração de ambiente (Fallback padrão para desenvolvimento local)
// Este arquivo é sobrescrito pelo Docker em tempo de execução via envsubst.
window.ENV = {
    API_BASE_URL: "http://localhost:8080/api"
};
